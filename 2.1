price = int(input("сколько стоит одна конфета? "))
count = int(input("сколько конфет ты хочешь купить? "))

total = price * count
weight = count * 25
packs = (count + 4) // 5

print("общая стоимость:", total)
print("вес в граммах:", weight)
print("нужно упаковок:", packs)
